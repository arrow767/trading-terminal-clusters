{
  "permissions": {
    "allow": [
      "Bash(ssh -o ConnectTimeout=15 clusters-lootx 'echo ALIVE')",
      "Bash(ssh -o ConnectTimeout=20 clusters-lootx 'echo ALIVE')",
      "Bash(ssh -o ConnectTimeout=20 clusters-lootx 'echo \"--- service status ---\"; systemctl is-active cluster-ingest 2>&1 || true; systemctl status cluster-ingest --no-pager -l 2>&1 | tail -15; echo \"--- recent logs ---\"; journalctl -u cluster-ingest -n 60 --no-pager 2>&1 | tail -50')",
      "Bash(ssh clusters-lootx 'set -e *)",
      "Bash(ssh clusters-lootx ' *)",
      "Bash(ssh clusters-lootx *)",
      "Bash(ssh -o *)"
    ]
  }
}
